services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: moneysplit
      POSTGRES_PASSWORD: moneysplit
      POSTGRES_DB: moneysplit
    ports:
      - "5432:5432"
    volumes:
      - moneysplit-pgdata:/var/lib/postgresql/data

volumes:
  moneysplit-pgdata:
